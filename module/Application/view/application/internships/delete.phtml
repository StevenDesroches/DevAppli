<?php


$title = 'Supprimer un stage';
$url   = $this->url('internships', ['action' => 'delete', 'id' => $id]);

$this->headTitle($title);
?>
<h1><?= $this->escapeHtml($title) ?></h1>

<p>
    Voulez-vous vraiment supprimer ce stage?
    <?= $this->escapeHtml($internship->id) ?>
    <?= $this->escapeHtml($internship->name) ?>
</p>

<form action="<?= $url ?>" method="post">
<div class="form-group">
    <input type="hidden" name="id" value="<?= (int) $internship->id ?>" />
    <input type="submit" class="btn btn-primary" name="del" value="Yes" />
    <input type="submit" class="btn btn-primary" name="del" value="No" />
</div>
</form>